/*using System;

namespace MyConsoleApp
{
    class Program
    {
        static void Main(string[] args)
        {
            string phrase = "t";//strings only use double quotes
            char oneWord = 'c';//only one word can be stored in a char keyword
            int age = 30;//this defines an integer

            //decimals can also be represented as float,double or decimal
            // they go in order of accuracy
            double gpa = 3.3;//double is one decimal place
            decimal score = 3;//decimal is to two decimal places
            bool isMale = true;//boolean is either true or false

            //a datatype not stored in a  variable is called a constant
            Console.WriteLine(false);


            Console.WriteLine(oneWord);//this prints c
            Console.WriteLine(score);
            Console.ReadLine();
        }
    }
}*/