/*using System;

namespace MyConsoleApp
{
    class Exeptions
    {

        static void Main(string[] args)
        {
            for (int i = 0; i < 3; i++)
            {
              // exceptions are used to exclude errors while a program during the runtime of a program
                try
                {
                    Console.Write("Enter a number: ");
                    int input = Convert.ToInt32(Console.ReadLine());
                }
                catch(FormatException error)// catch(eroor to catch[must not be added] variable)
                {
                    Console.WriteLine(error.Message);// variable can be used to print and more


                }
                //finally { 
                    // code goes here.(runs no matter what)
                //}   










        }
    }
}*/